自动释放池的作用：会在池子生命周期结束时，对池子中的对象发送一次release消息,这样可以防止僵尸对象的产生
autorelease方法：
1.是对象方法，返回对象本身
2.会将对象放到一个自动释放池子中。

@class 类名;用来声明一个类，但无法确定类中有什么内容

要解决循环引用问题：
须要修改某一个.h文件中的retain为assign,同时把.m中的dealloc中去掉release;

作业：三种象相互包含，
a->b->c->a;
