/*memcpy(,,);
memmove(char *dec, char *src, int count);
 
 dec的地址和src的地址一致
 dec的地址>src的地址（overlap);（count个字符先放到临时存储空间上
 dec的地址<src的地址
 

*/


#include<stdio.h>
#include<string.h>
int main(){
  /*  int a[]={1,2,3,4,5,8,9,0};
    int *s=a+2;
    memmove(s,a,5);//34345890
//    memcpy(s,a,5);
    for (int i=0; i<8; i++) {
        printf("%d\n",a[i]);
    }
   */
    
    
    
    int a[]={1,2,3,4,8,9,0};
    memmove
    
    
    
    
    return 0;
}